{
  "name": "mem-oracle",
  "version": "1.0.0",
  "description": "Documentation indexer and retrieval plugin for Claude Code",
  "author": "Jagjeevan Kashid (jagjeevanakashid@gmail.com)",
  "repository": "https://github.com/jagjeevanak/mem-oracle",
  "hooks": {
    "pre_prompt": {
      "command": "bun",
      "args": ["run", "src/plugin/hooks.ts", "pre-prompt"],
      "description": "Inject relevant documentation snippets into the prompt context"
    }
  },
  "settings": {
    "worker_url": {
      "type": "string",
      "default": "http://127.0.0.1:7432",
      "description": "URL of the mem-oracle worker service"
    },
    "top_k": {
      "type": "number",
      "default": 5,
      "description": "Number of snippets to retrieve"
    },
    "auto_index": {
      "type": "boolean",
      "default": true,
      "description": "Automatically index documentation URLs detected in prompts"
    }
  },
  "keywords": ["docs", "documentation", "retrieval", "rag", "indexer"]
}
